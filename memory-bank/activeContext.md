# Active Context: SceneFlow

## Current Focus
- Initializing the Memory Bank.
- Completing Phase 1 (Scaffolding) and moving into Phase 2/3 (Backend Core & Communication).

## Recent Changes
- Created `projectbrief.md`.
- Created `productContext.md`.
- Created `techContext.md` in `memory-bank/`.
- Set up the directory structure: `app/`, `electron/`, `frontend/`, `scripts/`.
- Implemented basic FastAPI backend with SQLite (via SQLAlchemy) and WebSocket telemetry.
- Implemented Electron shell and Vanilla/Alpine.js frontend structure.
- Configured `uv` for Python dependency management and `npm` for Node.js.

## Next Steps
1. Finalize Memory Bank (create `systemPatterns.md` and `progress.md`).
2. Implement refined backend logic for directory scanning (integrating `.sceneflow` management).
3. Connect the frontend to the backend for directory scanning and clip display.
4. Implement FFmpeg-based proxy generation.
